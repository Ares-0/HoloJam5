extends Room

# Cutscene steps:
# Wide, stars above gloom and bloom resting
# Darkness covers stars
# Zoom in on the two, darkness covers more
# Darkness (scratches) covers all but gloom
# Right click clears it (and ends rest state)
# But Bloom is gone
#
# Might need an extra song

# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta: float) -> void:
	pass
